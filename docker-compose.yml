# ============================================================
# BioNews - Docker Compose (Clustering & Reverse Proxy Ready)
# ============================================================
# Configurado para funcionar con Nginx Proxy Manager (NPM)
# y compatible con Docker Swarm / Stack.
#
# FASE 1 MICROSERVICIOS: Se añaden PostgreSQL y Redis como
# infraestructura de datos para la migración gradual desde SQLite.
# ============================================================

services:

  # ── PostgreSQL (Base de Datos Centralizada) ─────────────────
  postgres_db:
    image: postgres:15-alpine
    container_name: bionews-postgres
    restart: always
    environment:
      POSTGRES_USER: bionews_admin
      POSTGRES_PASSWORD: secret_master_password
      POSTGRES_DB: bionews_master
      POSTGRES_MULTIPLE_DATABASES: bionews_users_db,bionews_news_db,bionews_legal_db,bionews_consultations_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-multiple-databases.sh:/docker-entrypoint-initdb.d/init-multiple-databases.sh:ro
    ports:
      - "5432:5432"
    networks:
      - bionews-net
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U bionews_admin -d bionews_master" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    deploy:
      restart_policy:
        condition: on-failure
      placement:
        constraints:
          - node.role == manager

  # ── Redis (Message Broker / Caché) ──────────────────────────
  redis_broker:
    image: redis:7-alpine
    container_name: bionews-redis
    restart: always
    ports:
      - "6379:6379"
    networks:
      - bionews-net
    deploy:
      restart_policy:
        condition: on-failure
      placement:
        constraints:
          - node.role == manager

  # ── API Gateway Interno ──────────────────────────────────────────
  api:
    image: nginx:alpine
    container_name: bionews-gateway
    restart: unless-stopped
    volumes:
      - ./gateway/gateway.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - bionews-net
      - proxy_network # <- Conectado a la red de tu Nginx Proxy Manager
    expose:
      - "8000"

  # ── Microservicios FastAPI ───────────────────────────────────────
  auth-service:
    build: .
    container_name: bionews-auth-service
    command: uvicorn src.services.auth.main:app --host 0.0.0.0 --port 8001
    restart: unless-stopped
    volumes:
      - ./src:/app/src
      - /opt/BioNews/data:/app/data
      - /opt/BioNews/logs:/app/logs
      - /opt/BioNews/uploads:/app/uploads
      - /etc/localtime:/etc/localtime:ro
    networks:
      - bionews-net
    depends_on:
      postgres_db:
        condition: service_healthy
    environment:
      - POSTGRES_HOST=postgres_db
      - REDIS_URL=redis://redis_broker:6379/0

  news-service:
    build: .
    container_name: bionews-news-service
    command: uvicorn src.services.news.main:app --host 0.0.0.0 --port 8002
    restart: unless-stopped
    volumes:
      - ./src:/app/src
      - /opt/BioNews/data:/app/data
      - /opt/BioNews/logs:/app/logs
      - /etc/localtime:/etc/localtime:ro
    networks:
      - bionews-net
    depends_on:
      postgres_db:
        condition: service_healthy
    environment:
      - POSTGRES_HOST=postgres_db
      - REDIS_URL=redis://redis_broker:6379/0

  legal-service:
    build: .
    container_name: bionews-legal-service
    command: uvicorn src.services.legal.main:app --host 0.0.0.0 --port 8003
    restart: unless-stopped
    volumes:
      - ./src:/app/src
      - /opt/BioNews/data:/app/data
      - /opt/BioNews/logs:/app/logs
      - /etc/localtime:/etc/localtime:ro
    networks:
      - bionews-net
    depends_on:
      postgres_db:
        condition: service_healthy
    environment:
      - POSTGRES_HOST=postgres_db
      - REDIS_URL=redis://redis_broker:6379/0

  consultations-service:
    build: .
    container_name: bionews-consultations-service
    command: uvicorn src.services.consultations.main:app --host 0.0.0.0 --port 8004
    restart: unless-stopped
    volumes:
      - ./src:/app/src
      - /opt/BioNews/data:/app/data
      - /opt/BioNews/logs:/app/logs
      - /etc/localtime:/etc/localtime:ro
    networks:
      - bionews-net
    depends_on:
      postgres_db:
        condition: service_healthy
    environment:
      - POSTGRES_HOST=postgres_db
      - REDIS_URL=redis://redis_broker:6379/0
  # ── Scheduler de Scrapers ───────────────────────────────────
  scheduler:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bionews-scheduler
    restart: unless-stopped
    command: [ "python", "scheduler.py" ]
    volumes:
      - /opt/BioNews/data:/app/data
      - /opt/BioNews/logs:/app/logs
      - /etc/localtime:/etc/localtime:ro
    networks:
      - bionews-net
    depends_on:
      postgres_db:
        condition: service_healthy
    environment:
      - TZ=America/Santiago
      - POSTGRES_HOST=postgres_db
      - POSTGRES_PORT=5432
      - POSTGRES_USER=bionews_admin
      - POSTGRES_PASSWORD=secret_master_password
      - REDIS_URL=redis://redis_broker:6379/0
    deploy:
      restart_policy:
        condition: on-failure
      placement:
        constraints:
          - node.role == manager

  # ── Frontend (Nginx + React build) ──────────────────────────
  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    container_name: bionews-web
    restart: unless-stopped
    # Puertos: Se eliminan (3080:80) para usar el Reverse Proxy de NPM
    networks:
      - bionews-net
      - proxy_network
    depends_on:
      api:
        condition: service_started
    deploy:
      restart_policy:
        condition: on-failure
      placement:
        constraints:
          - node.role == manager

# Volúmenes
volumes:
  postgres_data:
    driver: local

# Redes
networks:
  # Red interna para comunicación privada entre servicios de BioNews
  bionews-net:
    driver: bridge
  # Red externa compartida con Nginx Proxy Manager
  proxy_network:
    external: true
